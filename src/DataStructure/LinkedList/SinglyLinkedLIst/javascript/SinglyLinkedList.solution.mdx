import { Meta, Canvas, Story } from '@storybook/addon-docs';

import SinglyLinkedList from './singlyLinkedList.es3.cjs';
import code from './singlyLinkedList.es3.cjs?raw';
import runTestCase, { cases } from './testCases.js';

export const c0 = cases[0];

<Meta title="Data Structure/Linked List/Singly Linked List/Solution/Javascript" />

# Singly Linked List

Modify the solution file `./SinglyLinkedList.es3.cjs` to test results.

## Default Case

<Canvas withSource="open">
  <Story name="Case 0" parameters={{ docs: { source: { code } } }}>
    <div>
      <div>Input: </div>
      <div>{JSON.stringify(c0.commands)}</div>
    </div>
    <br />
    <div>
      <div>Output: </div>
      <div>{JSON.stringify(runTestCase(SinglyLinkedList, c0))}</div>
    </div>
  </Story>
</Canvas>
