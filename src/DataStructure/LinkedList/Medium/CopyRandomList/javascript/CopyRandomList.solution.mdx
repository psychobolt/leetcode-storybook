import cloneDeep from 'lodash/fp/cloneDeep.js';
import memoize from 'lodash/fp/memoize.js';
import { Meta, Story, Canvas } from '@storybook/addon-docs';

import solution from './copyRandomList.es3.cjs';
import code from './copyRandomList.es3.cjs?raw';
import runTestCase, { RandomLinkedList, cases } from './testCases.js';

export const c0 = cases[0];
export const printOutput = memoize(() => {
  const list = new RandomLinkedList();
  list.head = runTestCase(solution, cloneDeep(c0));
  return JSON.stringify(list.toArray());
});

<Meta title="Data Structure/Linked List/Medium/[138] Copy List with Random Pointer/Solution/Javascript" />

# Solution

Modify the solution file `./copyRandomList.es3.cjs` to test results.

## Default Case

<Canvas withSource="open">
  <Story name="Case 0" parameters={{ docs: { source: { code } } }}>
    <div>
      <div>Input: </div>
      <pre>{JSON.stringify(c0.input)}</pre>
    </div>
    <br />
    <div>
      <div>Output: </div>
      <pre>{printOutput()}</pre>
    </div>
  </Story>
</Canvas>
