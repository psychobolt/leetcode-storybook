import cloneDeep from 'lodash/fp/cloneDeep.js';
import { Meta, Story, Canvas } from '@storybook/addon-docs';

import solution from './removeElement.es3.cjs';
import code from './removeElement.es3.cjs?raw';
import runTestCase, { cases } from './testCases.js';

export const c0 = cases[0];
export const testCase = cloneDeep(c0);

<Meta title="Data Structure/Array/1D/Easy/[27] Remove Element/Solution/Javascript" />

# Solution

Modify the solution file `./removeElement.es3.cjs` to test results.

## Default Case

<Canvas withSource="open">
  <Story name="Case 0" parameters={{ docs: { source: { code } } }}>
    <div>
      <div>Input: </div>
      <div>{JSON.stringify(c0.input)}</div>
    </div>
    <br />
    <div>
      <div>Output: </div>
      <div>{JSON.stringify(runTestCase(solution, testCase))}</div>
      <div>{JSON.stringify(testCase.input.nums)}</div>
    </div>
  </Story>
</Canvas>
