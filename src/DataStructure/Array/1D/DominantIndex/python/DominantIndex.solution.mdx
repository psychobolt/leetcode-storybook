import { Meta, Story, Canvas } from '@storybook/addon-docs';

import solution from './dominantIndex.py?file';
import code from './dominantIndex.py?raw';
import json from '../testCases.json?file';
import testCases from './testCases.py?file';

export const c0 = 'cases[0]';

<Meta title="Data Structure/Array/1D/[747] Largest Number At Least Twice of Others/Solution/Python" />

# Solution

Modify the solution file `./dominantIndex.py` to test results.

## Default Case

<Canvas withSource="open">
  <Story name="Case 0" parameters={{ docs: { source: { code } } }} inline={false} height="130px">
    <py-env>
    {`
      - paths:
        - ${json}
        - ${testCases}
        - ${solution}
    `}
    </py-env>
    <div>
      <div>Input: </div>
      <py-script>
      {`
        from testCases import cases
        print(${c0}['input'])
      `}
      </py-script>
    </div>
    <br />
    <div>
      <div>Output: </div>
      <py-script>
      {`
        from testCases import cases, runTestCase
        from dominantIndex import Solution
        solution = Solution()
        print(runTestCase(solution.dominantIndex, ${c0}))
      `}
      </py-script>
    </div>
  </Story>
</Canvas>
