import { Meta, Story, Canvas } from '@storybook/addon-docs';

import solution from './runningSum.es3.cjs';
import code from './runningSum.es3.cjs?raw';
import runTestCase, { cases } from './testCases.js';

export const c0 = cases[0];

<Meta title="Data Structure/Array/1D/[1480] Running Sum of 1d Array/Solution/Javascript" />

# Solution

Modify the solution file `./runningSum.es3.cjs` to test results.

## Default Case

<Canvas withSource="open">
  <Story name="Case 0" parameters={{ docs: { source: { code } } }}>
    <div>
      <div>Input: </div>
      <div>{JSON.stringify(c0.input)}</div>
    </div>
    <br />
    <div>
      <div>Output: </div>
      <div>{JSON.stringify(runTestCase(solution, c0))}</div>
    </div>
  </Story>
</Canvas>
