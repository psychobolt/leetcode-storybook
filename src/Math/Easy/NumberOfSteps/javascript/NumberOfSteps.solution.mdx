import cloneDeep from 'lodash/fp/cloneDeep.js';
import { Meta, Story, Canvas } from '@storybook/addon-docs';

import solution from './numberOfSteps.es3.cjs';
import code from './numberOfSteps.es3.cjs?raw';
import runTestCase, { cases } from './testCases.js';

export const c0 = cases[0];
export const testCase = cloneDeep(c0);

<Meta title="Math/Easy/[1342] Number of Steps to Reduce a Number to Zero/Solution/Javascript" />

# Solution

Modify the solution file `./numberOfSteps.es3.cjs` to test results.

## Default Case

<Canvas withSource="open">
  <Story name="Case 0" parameters={{ docs: { source: { code } } }}>
    <div>
      <div>Input: </div>
      <div>{JSON.stringify(c0.input)}</div>
    </div>
    <br />
    <div>
      <div>Output: </div>
      <div>{JSON.stringify(runTestCase(solution, testCase))}</div>
    </div>
  </Story>
</Canvas>
